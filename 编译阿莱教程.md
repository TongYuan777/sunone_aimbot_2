# 一步步编译出 "阿# 一步步编译出 "阿莱"（ai.exe）中文教程

# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sun# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。
# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

#### 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
   - 安装时必须勾选：**“使用 C# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
   - 安装时必须勾选：**“使用 C++ 的桌面开发”** 工作负载# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
   - 安装时必须勾选：**“使用 C++ 的桌面开发”** 工作负载
3. **Windows SDK**
4. **CMake**（建议 3.20+# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
   - 安装时必须勾选：**“使用 C++ 的桌面开发”** 工作负载
3. **Windows SDK**
4. **CMake**（建议 3.20+）
5. **PowerShell**（Windows 自带即可）
6. **# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
   - 安装时必须勾选：**“使用 C++ 的桌面开发”** 工作负载
3. **Windows SDK**
4. **CMake**（建议 3.20+）
5. **PowerShell**（Windows 自带即可）
6. **Git**（用于克隆仓库）

# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
   - 安装时必须勾选：**“使用 C++ 的桌面开发”** 工作负载
3. **Windows SDK**
4. **CMake**（建议 3.20+）
5. **PowerShell**（Windows 自带即可）
6. **Git**（用于克隆仓库）

### 2.2 仅 CUDA 版本需要

- **CUDA Toolkit 13.# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
   - 安装时必须勾选：**“使用 C++ 的桌面开发”** 工作负载
3. **Windows SDK**
4. **CMake**（建议 3.20+）
5. **PowerShell**（Windows 自带即可）
6. **Git**（用于克隆仓库）

### 2.2 仅 CUDA 版本需要

- **CUDA Toolkit 13.1 或更高版本**
  -# 一步步编译出 "阿莱"（ai.exe）中文教程

> 本文档教你从零开始编译 `sunone_aimbot_2` 项目，最终生成可执行文件 `ai.exe`（圈内常称“阿莱”）。

---

## 一、先确认你的目标

本项目有 **两个后端版本**，编译前必须二选一：

| 后端 | 简称 | 适用显卡 | 模型格式 | 性能 | 复杂度 |
|---|---|---|---|---|---|
| **DirectML** | DML | 任意显卡（NVIDIA/AMD/Intel/核显） | `.onnx` | 中等 | 简单 |
| **CUDA + TensorRT** | CUDA | NVIDIA RTX 2000/3000/4000/5000 等 | `.engine` 或 `.onnx` | 最高 | 复杂 |

**建议：**
- 老显卡（GTX 10 系及更早）→ 选 **DML**
- 新 NVIDIA 显卡且追求极致速度 → 选 **CUDA**

---

## 二、准备编译环境

### 2.1 必须安装（两个版本都需要）

1. **Windows 10 或 Windows 11（64 位）**
2. **Visual Studio 2022** 或 **Visual Studio Build Tools**
   - 安装时必须勾选：**“使用 C++ 的桌面开发”** 工作负载
3. **Windows SDK**
4. **CMake**（建议 3.20+）
5. **PowerShell**（Windows 自带即可）
6. **Git**（用于克隆仓库）

### 2.2 仅 CUDA 版本需要

- **CUDA Toolkit 13.1 或更高版本**
  - 推荐 CUDA 13.2
  -